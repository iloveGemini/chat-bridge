# ── 密钥 / 配置（含 API key，本地保留，切勿提交）──
config.json

# ── 运行时 & 个人数据（聊天记录 / 记忆库 / 上传图片 / 归档）──
data/
_backups/
chat_summary.md
input.txt
to do(considering).txt
MEMORY_REFACTOR_PLAN(done).md
.pending

# ── 日志 ──
*.log

# ── Python ──
__pycache__/
*.pyc
*.pyo

# ── Claude Code 本地设置（仅本机权限白名单，不通用）──
.claude/

# 备注：data/prompts/ 默认会被提交（你的角色/世界/预设库，自用备份）。
# 若觉得里面的角色设定太私人，把下面一行取消注释即可一并排除：
# data/prompts/
